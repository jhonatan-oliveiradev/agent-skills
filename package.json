{
  "name": "jhonatan-agent-skills",
  "version": "1.0.0",
  "private": true,
  "description": "Curated Agent Skills for frontend, product design, motion, backend and data, architecture, quality, application security, engineering workflow, brand design, writing and communication, delivery, and game-development workflows.",
  "scripts": {
    "validate": "node scripts/validate-skills.mjs && node scripts/validate-catalog.mjs && node scripts/generate-catalog.mjs --check && node scripts/validate-plugin.mjs",
    "validate:skills": "node scripts/validate-skills.mjs",
    "validate:catalog": "node scripts/validate-catalog.mjs",
    "validate:plugin": "node scripts/validate-plugin.mjs",
    "catalog:generate": "node scripts/generate-catalog.mjs",
    "catalog:check": "node scripts/generate-catalog.mjs --check",
    "test": "node --test scripts/*.test.mjs scripts/lib/*.test.mjs",
    "web:test": "npm --prefix apps/web test",
    "web:typecheck": "npm --prefix apps/web run typecheck",
    "web:lint": "npm --prefix apps/web run lint",
    "web:build": "npm --prefix apps/web run build",
    "setup:project": "node scripts/bootstrap-project.mjs",
    "create:web-app": "node scripts/create-web-app.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
